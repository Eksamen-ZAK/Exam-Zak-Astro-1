---
interface Props {
  text: string;
  link: string;
  image: string;
  id?: string;
}
const { link, image, text, id } = Astro.props;
---

<button class="menu-item" data-link={link} id={id}>
  <div class={image}></div>
  <p>{text}</p>
</button>

<script>
  const buttons = document.querySelectorAll(".menu-item");
  buttons.forEach((button) => {
    const href = button.getAttribute("data-link");
    button.addEventListener("mousedown", () => {
      if (href === "/") {
        sessionStorage.setItem("uuid", "");
        localStorage.setItem("uuid", "");
      }
      window.location.href = `${href}`;
      if (sessionStorage.getItem("program-list")) {
        sessionStorage.setItem("program-title", "");
        sessionStorage.setItem("program-id", "");
        sessionStorage.setItem("program-list", "");
        sessionStorage.setItem("program-description", "");
      }
    });
    button.addEventListener("keypress", function (e) {
      if (e.key === "Enter") {
        window.location.href = `${href}`;
        if (sessionStorage.getItem("program-list")) {
          sessionStorage.setItem("program-title", "");
          sessionStorage.setItem("program-id", "");
          sessionStorage.setItem("program-list", "");
          sessionStorage.setItem("program-description", "");
        }
      }
    });
  });
</script>

<style>
  button {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    text-decoration: none;
    cursor: pointer;
    border: none;
    background-color: transparent;
  }
  p {
    color: var(--main-bg-color);
    font-family: var(--main-font);
    font-size: var(--fs-l);
  }
  .home-icon {
    width: 140px;
    height: 125px;
    padding: 2px;
    background-image: url(/assets/home-icon.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .home-icon:hover {
    width: 150px;
    height: 125px;
    padding: 2px;
    background-image: url(/assets/home-filled-icon.svg);
  }
  .build-icon {
    width: 125px;
    height: 125px;
    padding: 2px;
    background-image: url(/assets/build-icon.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .profile-icon {
    width: 125px;
    height: 125px;
    margin-left: 10px;
    padding: 2px;
    background-image: url(/assets/profile-icon.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .profile-icon:hover {
    width: 125px;
    height: 125px;
    margin-left: 10px;
    padding: 2px;
    background-image: url(/assets/profile-filled.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .build-icon:hover {
    width: 125px;
    height: 125px;
    padding: 2px;
    background-image: url(/assets/build-filled-icon.svg);
  }
  .bookmark-icon {
    width: 93px;
    height: 121px;
    padding: 2px;
    background-image: url(/assets/bookmark-icon.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .bookmark-icon:hover {
    width: 93px;
    height: 121px;
    padding: 2px;
    background-image: url(/assets/bookmark-filled-icon.svg);
  }
  .stats-icon {
    width: 120px;
    height: 123px;
    padding: 2px;
    background-image: url(/assets/stats-icon2.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .stats-icon:hover {
    width: 120px;
    height: 123px;
    padding: 2px;
    background-image: url(/assets/stats-filled-icon.svg);
  }
  .about-us-icon {
    width: 153px;
    height: 110px;
    padding: 2px;
    background-image: url(/assets/about-us-icon.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .about-us-icon:hover {
    width: 153px;
    height: 110px;
    padding: 2px;
    background-image: url(/assets/about-us-filled-icon.svg);
  }
  .settings-icon {
    width: 111px;
    height: 121px;
    padding: 2px;
    background-image: url(/assets/settings-icon.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .settings-icon:hover {
    width: 111px;
    height: 121px;
    padding: 2px;
    background-image: url(/assets/settings-filled-icon.svg);
  }
  .exit-icon {
    width: 125px;
    height: 125px;
    padding: 2px;
    margin-left: 20px;
    background-image: url(/assets/exit-icon.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
  .exit-icon:hover {
    width: 125px;
    height: 125px;
    padding: 2px;
    margin-left: 20px;
    background-image: url(/assets/exit-filled.svg);
    background-repeat: no-repeat;
    background-size: contain;
  }
</style>
